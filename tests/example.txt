// Площадь прямоугольника
function rect(a, b) {
    s = a * b;
    return s;
}

// Площадь круга
function cir(a, p) {
    s = p * a * a;
    return s;
}

// Решим задачу
// Вычислить площадь фигуры которая получится из исходного материала
// Исходный материал квадрат 10 на 10
// Фигура круг с радиусом 2 вписанный в рамку с толщиной 2
// Если будет использован не весь материал нужно посчитать соколько прямоугольников размером 2 на 2 можно будет из них сделать
r = rect(10, 10) - rect(8, 8) + cir(2, 3);

if (r < rect(10, 10)) {
    o = (rect(8, 8) - rect(4, 4)) / rect(2, 2);
}
